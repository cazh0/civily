#Pins the JVM the Gradle daemon runs on.
#Why: the Android Gradle Plugin rejects JVMs newer than it knows about, so whichever JDK a
#contributor happens to have on PATH decides whether the build works. Declaring it here makes
#the build independent of the machine.
toolchainVersion=21
